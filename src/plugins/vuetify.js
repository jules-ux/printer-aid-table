import Vuetify from 'vuetify';
import 'vuetify/styles'; // Import Vuetify styles
import { createVuetify } from 'vuetify';

export default createVuetify({
  // Vuetify configuration options (optional)
});
